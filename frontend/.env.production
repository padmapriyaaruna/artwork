# Production API URL — baked into the Vite build at compile time
# This file is committed to git so Render always uses the correct backend URL
VITE_API_URL=https://artwork-engine-backend.onrender.com/api
